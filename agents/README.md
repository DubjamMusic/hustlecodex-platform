# Wave B Action Figures

Machine roster: `roster.json`  
Router: `dispatch.ts`  
Tests: `dispatch.test.ts` (`node --experimental-strip-types agents/dispatch.test.ts`)

Do not add a sixth generic "orchestrator" figure. Route through Relay instead.
